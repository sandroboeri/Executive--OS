import type { Commitment } from "@/types/review";
type Props = { completed: number; total: number; commitments: Commitment[]; bigThree: string[]; positiveClose: string };
export function ExecutiveBrief({ completed, total, commitments, bigThree, positiveClose }: Props) {
  const active = commitments.filter((item) => item.status !== "Done");
  return <section className="rounded-3xl bg-white p-5 card-shadow"><h2 className="text-xl font-bold text-slate-950">Executive Brief</h2><div className="mt-4 grid grid-cols-2 gap-3"><Metric label="Steps" value={`${completed}/${total}`} /><Metric label="Captured" value={commitments.length.toString()} /><Metric label="Open / Waiting" value={active.length.toString()} /><Metric label="Priorities" value={bigThree.filter(Boolean).length.toString()} /></div><div className="mt-5"><h3 className="text-sm font-bold uppercase tracking-[0.18em] text-slate-500">Big Three</h3><ol className="mt-3 list-decimal space-y-2 pl-5 text-sm text-slate-700">{bigThree.filter(Boolean).length === 0 ? <li className="text-slate-400">Not set yet</li> : bigThree.filter(Boolean).map((item) => <li key={item}>{item}</li>)}</ol></div>{positiveClose && <div className="mt-5"><h3 className="text-sm font-bold uppercase tracking-[0.18em] text-slate-500">Positive Close</h3><p className="mt-3 whitespace-pre-wrap rounded-2xl bg-slate-50 p-4 text-sm text-slate-700">{positiveClose}</p></div>}</section>;
}
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl bg-slate-50 p-4"><p className="text-2xl font-bold text-slate-950">{value}</p><p className="mt-1 text-xs text-slate-500">{label}</p></div>; }
