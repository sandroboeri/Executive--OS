import type { CommitmentPriority, CommitmentStatus } from "@/types/review";
type Draft = { title: string; due: string; priority: CommitmentPriority; status: CommitmentStatus; notes: string };
type Props = { draft: Draft; onChange: (draft: Draft) => void; onAdd: () => void };
export function CommitmentForm({ draft, onChange, onAdd }: Props) {
  return <section className="mt-6 rounded-3xl border border-slate-200 bg-white p-5"><h3 className="text-lg font-bold text-slate-950">Add to Commitment Register</h3><div className="mt-4 grid gap-3"><input value={draft.title} onChange={(event) => onChange({ ...draft, title: event.target.value })} placeholder="Action, preparation task, waiting-for item, or follow-up" className="rounded-2xl border border-slate-200 px-4 py-3 outline-none focus:border-slate-500" /><div className="grid gap-3 sm:grid-cols-3"><input value={draft.due} onChange={(event) => onChange({ ...draft, due: event.target.value })} placeholder="Due date" className="rounded-2xl border border-slate-200 px-4 py-3 outline-none focus:border-slate-500" /><select value={draft.priority} onChange={(event) => onChange({ ...draft, priority: event.target.value as CommitmentPriority })} className="rounded-2xl border border-slate-200 px-4 py-3 outline-none focus:border-slate-500"><option>High</option><option>Medium</option><option>Low</option></select><select value={draft.status} onChange={(event) => onChange({ ...draft, status: event.target.value as CommitmentStatus })} className="rounded-2xl border border-slate-200 px-4 py-3 outline-none focus:border-slate-500"><option>Open</option><option>Waiting</option><option>Done</option></select></div><textarea value={draft.notes} onChange={(event) => onChange({ ...draft, notes: event.target.value })} placeholder="Notes" rows={3} className="rounded-2xl border border-slate-200 px-4 py-3 outline-none focus:border-slate-500" /><button type="button" onClick={onAdd} className="rounded-2xl bg-slate-950 px-4 py-3 font-semibold text-white hover:bg-slate-800">Add Commitment</button></div></section>;
}
