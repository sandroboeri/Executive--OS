import { Trash2 } from "lucide-react";
import type { Commitment } from "@/types/review";
type Props = { commitments: Commitment[]; onDelete: (id: string) => void };
export function CommitmentRegister({ commitments, onDelete }: Props) {
  const active = commitments.filter((item) => item.status !== "Done");
  return <section className="rounded-3xl bg-white p-5 card-shadow"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-bold text-slate-950">Commitment Register</h2><p className="mt-1 text-sm text-slate-500">{active.length} open or waiting</p></div><div className="rounded-full bg-slate-100 px-3 py-1 text-sm font-semibold text-slate-700">{commitments.length}</div></div><div className="mt-4 space-y-3">{commitments.length === 0 ? <p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-500">Nothing captured yet. Add commitments as you review each source.</p> : commitments.map((item) => <article key={item.id} className="rounded-2xl border border-slate-200 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-950">{item.title}</p><p className="mt-1 text-xs text-slate-500">{item.source} · {item.priority} · {item.status}</p>{item.due && <p className="mt-1 text-xs text-slate-500">Due: {item.due}</p>}{item.notes && <p className="mt-2 text-sm text-slate-600">{item.notes}</p>}</div><button type="button" onClick={() => onDelete(item.id)} className="rounded-lg p-1 text-slate-400 hover:bg-slate-100 hover:text-slate-700" aria-label="Delete commitment"><Trash2 className="h-4 w-4" /></button></div></article>)}</div></section>;
}
